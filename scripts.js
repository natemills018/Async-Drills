getWordOfTheDay()
.then((word) => {
    console.log(`The word of the day is ${word}.`)
})


function orderLunch(name) {
    return new Promise((resolve, reject) => {
        setTimeout(() => {
            
        }
    }   
}